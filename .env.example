# .env.example
# =========================================
# 概要:
# - 環境変数の例（実値は入れない）
# - このファイルを .env にコピーして利用する
#
# 契約:
# - キー名はこのファイルに揃える（追加/変更時は .env.example も更新）
# - 値は環境ごとに .env / Secret Manager 等で差し替える
#
# 前提:
# - Docker Compose は開発用のみ（本番は Dockerfile でビルド/実行）
# =========================================

# -----------------------------------------
# Docker Compose settings (開発用)
# - 本番では、compose.ymlは使用せず、Dockerfileで直接ビルドする
# -----------------------------------------

# デフォルトでdevプロファイルを有効化（docker compose upで開発用コンテナが立ち上がる）
COMPOSE_PROFILES=dev

# ホスト側の公開ポート
WEB_DEV_HOST_PORT=3000
WEB_PROD_HOST_PORT=3000
